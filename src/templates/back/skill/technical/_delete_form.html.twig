<form method="post" action="{{ path('admin.skill.technical.delete', {'id': technical_skill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technical_skill.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
