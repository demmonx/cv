{% extends 'back/layout/base.html.twig' %}
{% block title_section %}My hobbies{% endblock %}
{% block title %}Hobbies{% endblock %}

{% block title_content %}Hobby : {{ hobby.name }}{% endblock %}

{% block body %}
    <blockquote>
        <p>{{ hobby.text }}</p>
        <small>{{ hobby.name }}</small>
    </blockquote>

    <ul class="options">
            <li><a href="{{ path('admin.hobby.index') }}"><button class='btn btn-warning'>Go back</button></a></li>
            <li> <a href="{{ path('admin.hobby.edit', {'id': hobby.id}) }}"><button class='btn btn-success'>Edit</button></a></li>
            <li>{{ include('back/hobby/_delete_form.html.twig') }}</li>
        </ul>

{% endblock %}
