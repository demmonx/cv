<form method="post" action="{{ path('admin.user.extra.delete', {'id': extra_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extra_info.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
